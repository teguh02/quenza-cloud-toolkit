# Quenza Cloud Toolkit - Phase 1 dependencies

# Web framework & server
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Templating & form handling
jinja2==3.1.5
python-multipart==0.0.20

# Configuration
pydantic-settings==2.7.1
python-dotenv==1.0.1

# Session & security
itsdangerous==2.2.0
bcrypt==4.2.1

# Database (SQLite via SQLAlchemy)
SQLAlchemy==2.0.50

# Scheduling (Phase 4)
APScheduler==3.11.0

# Cloud destinations
boto3==1.35.90
# Google Drive (OAuth)
google-auth==2.37.0
google-auth-oauthlib==1.2.1
google-api-python-client==2.157.0
# SCP / SFTP (transfer over SSH). FTP uses the stdlib ftplib.
paramiko==3.5.0

# Secrets encryption (refresh tokens, etc.)
cryptography==44.0.0

# Dev/testing (TestClient)
httpx==0.28.1
